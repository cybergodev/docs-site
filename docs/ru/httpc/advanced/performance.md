---
title: Оптимизация производительности - HTTPC
description: "Руководство по оптимизации производительности HTTPC. Подробное сравнение производственных характеристик четырёх предустановок Default, Secure, Performance и Minimal, настройка параметров пула соединений ConnectionConfig, стратегия повторного использования Result через пул объектов и выявление распространённых антипаттернов производительности с решениями"
---

# Оптимизация производительности

## Сравнение предустановленных конфигураций

| Показатель | Default | Secure | Performance | Minimal |
|------|---------|--------|-------------|---------|
| Таймаут Request | 30s | 15s | 60s | 30s |
| MaxIdleConns | 50 | 20 | 100 | 10 |
| MaxConnsPerHost | 10 | 5 | 20 | 2 |
| MaxRetries | 3 | 1 | 3 | 0 |
| MaxResponseBodySize | 10MB | 5MB | 50MB | 1MB |
| HTTP/2 | Включено | Включено | Включено | Включено |
| Cookies | Отключено | Отключено | Включено | Отключено |
| Защита от SSRF | Включено | Включено | Включено | Включено |
| FollowRedirects | Включено | Отключено | Включено | Отключено |

## Выбор конфигурации по сценарию

| Сценарий | Рекомендуемая предустановка | Рекомендации по настройке |
|------|----------|----------|
| Универсальный веб-сервис | Default | - |
| Обработка URL-адресов от пользователей | Secure | - |
| Внутренние микросервисы с высокой нагрузкой | Performance | Увеличить MaxIdleConns |
| Одноразовые скрипты | Minimal | - |
| Сервис загрузки файлов | Performance | Увеличить MaxResponseBodySize |
| Финансовые/медицинские API | Secure + пользовательская | Добавить промежуточное ПО аудита |

```go
// Сценарий с высокой пропускной способностью
client, _ := httpc.New(httpc.PerformanceConfig())

// Тонкая настройка на основе предустановки
cfg := httpc.PerformanceConfig()
cfg.Timeouts.Request = 120 * time.Second
cfg.Connection.MaxIdleConns = 200
client, _ := httpc.New(cfg)
```

## Повторное использование через пул объектов

HTTPC имеет встроенный пул объектов Result. Используйте `ReleaseResult` для возврата:

```go
result, err := client.Get(url)
if err != nil {
    return err
}
defer httpc.ReleaseResult(result) // Возврат в пул объектов
```

:::tip Подсказка
В сценариях с высоким параллелизмом `ReleaseResult` значительно снижает нагрузку на сборщик мусора.
:::

## Антипаттерны производительности

| Антипаттерн | Причина | Правильный подход |
|--------|------|----------|
| Создание клиента для каждого запроса | Соединения не могут быть переиспользованы | Глобальное переиспользование клиента |
| Игнорирование ReleaseResult | Увеличение нагрузки на GC | Возврат через defer |
| Слишком большой MaxResponseBodySize | Высокое потребление памяти | Установите разумные ограничения |
| Использование result.String() на горячих путях | Издержки на создание строки | Используйте Body() напрямую |

## Что дальше

- [Пул соединений и прокси](./connection-pool) — выбор параметров пула соединений, настройка прокси и DoH
- [Обработка ошибок](./error-handling) — стратегия многоуровневых таймаутов
- [Обзор безопасности](../security/) — баланс между безопасностью и производительностью
